<!--
  LIGHT_CONTEXT.md — the live, compact context file for the CURRENT LIGHT-mode milestone.
  Lives at .ai-company/LIGHT_CONTEXT.md (one active copy at a time). Read WORKFLOW-LIGHT.md for
  how this file is used and archived. Top-level fields are mutable and always reflect current
  state; the Change log at the bottom is append-only.

  On bootstrap, copy this template to .ai-company/LIGHT_CONTEXT.md and delete this comment block.
-->

# LIGHT_CONTEXT — Milestone {{N}} — {{short title}}

Mode: LIGHT
Status: DRAFT
<!-- Status values: DRAFT | CEO-APPROVED | IN-DEV | IN-QA | ESCALATED-TO-FULL |
     RELEASE-APPROVED | RELEASED -->

## Goal

{{one paragraph, plain language — what this milestone does and why}}

## Acceptance criteria

1. {{testable statement — "given X input, the system does Y, verifiable by Z"}}
2. ...

## Files allowed to change

- {{path/to/file}}
- ...

## Files prohibited from changing

- {{path/to/file}} — {{why, if not obvious}}
- ...

## Architecture constraints

- {{any structural rule the Developer must follow — existing patterns to match, interfaces not to
  break, etc. If a real architecture decision is needed here, stop and escalate to FULL mode
  instead of filling this in as if it were settled.}}

## Latest relevant commit hashes

- {{hash}} — {{one-line description}}

## Known open defects / risks

- {{anything already known going in — pre-existing issues, deferred items, things QA should watch}}

## Test command

{{TEST_COMMAND}} — see `.ai-company/TESTING_STANDARDS.md` for the fallback method if none exists.

## Handoff status

- Current stage: {{e.g. "Awaiting CEO approval of scope"}}
- Next role: {{role}}
- Explicit stop point: {{one line, per HANDOFF_PROTOCOL.md}}

---

## Change log (append-only — one line per stage transition)

- {{date}} — CEO — scope drafted
